"""Bar chart — builds instance arrays for dr2d SDF rounded rects (placeholder)."""


def bar(x, y, *, color=None, width=0.8, opacity=1.0):
    """Create a bar chart from x/y data.

    Args:
        x: array-like of category positions
        y: array-like of bar heights
        color: RGB tuple (0-1)
        width: bar width as fraction of spacing
        opacity: opacity (0-1)

    Returns:
        Instance array ready for dr2d rendering.
    """
    # TODO: Phase 2.2
    raise NotImplementedError("Bar chart not yet implemented")
