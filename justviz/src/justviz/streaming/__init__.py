"""Streaming data — append-only, sliding window, live updates (placeholder)."""

# TODO: Phase 5
# - chart.push_data(new_rows) — append new data points
# - Partial vertex buffer updates (no full rebuild)
# - Sliding window with ring buffer
# - Data source adapters: WebSocket, file polling, Python generator
